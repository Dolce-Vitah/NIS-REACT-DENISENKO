# E-Commerce Admin Panel

## Инструкция по запуску

### Установка и запуск

1. **Клонируйте репозиторий и перейдите в папку проекта:**
   ```bash
   cd e-commerce-admin
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите сервер для разработки:**
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу: `http://localhost:5173`

4. **Сборка для продакшена:**
   ```bash
   npm run build
   ```

5. **Проверка линтером:**
   ```bash
   npm run lint
   ```

---

## 🛠 Технологический стек
- **Core:** React 19, TypeScript
- **State Management:** Redux Toolkit, Redux Persist
- **Data Fetching:** RTK Query (API: DummyJSON)
- **Routing:** React Router v7
- **Styling:** Tailwind CSS
- **Animations:** Framer Motion
- **Forms & Validation:** React Hook Form, Zod
- **i18n:** react-i18next
- **Notifications:** Sonner
- **Architecture:** Feature-Sliced Design (FSD)

---

## Чек-лист выполненных основных требований

- [x] **1.1 Аутентификация и авторизация**
  - [x] Страница логина с валидацией
  - [x] Авторизация через RTK Query (`POST /auth/login`)
  - [x] Хранение токена и данных пользователя в Redux (auth slice)
  - [x] Защищенные маршруты (редирект на `/login` для неавторизованных)
  - [x] Автоматическая инициализация пользователя при перезагрузке (`GET /auth/me`)
  - [x] Logout (очистка сессии и кэша RTK Query)
  - [x] UI-заглушка для регистрации
- [x] **1.2 Маршрутизация и layout**
  - [x] Разделение публичных (`/login`, `/register`) и приватных маршрутов
  - [x] Общий layout для приватной части (Sidebar + Header)
  - [x] Lazy loading страниц (Suspense)
  - [x] Обработка 404 ошибки
- [x] **1.3 Работа с продуктами**
  - [x] Список продуктов с пагинацией (limit / skip)
  - [x] Поиск по названию (с debounce)
  - [x] Просмотр детальной страницы продукта
  - [x] Все запросы через RTK Query с кэшированием
  - [x] Обработка состояний: loading / error / empty state
- [x] **1.4 Профиль пользователя**
  - [x] Отображение данных (имя, email, аватар)
  - [x] Кнопка выхода из системы
- [x] **1.5 Настройки приложения**
  - [x] Переключение языка (ru / en)
  - [x] Переключение темы (light / dark)
  - [x] Настройка размера страницы каталога
  - [x] Сохранение настроек через Redux Persist (localStorage)
- [x] **1.6 Интернационализация (i18n)**
  - [x] Перевод всех страниц, форм, ошибок API и пустых состояний
  - [x] Локализация всех toast-уведомлений и новых UX-элементов (модалка удаления, breadcrumbs, секции sidebar, Actions)
  - [x] Переключение "на лету" без перезагрузки страницы
- [x] **Архитектура**
  - [x] Строгий TypeScript
  - [x] Feature-Sliced Design (app, pages, widgets, features, entities, shared)
  - [x] Изолированный API-слой и переиспользуемые UI-компоненты

---

## Реализованные улучшения и их обоснование

Проект был значительно расширен по сравнению с базовым ТЗ:


### 1. Продвинутые анимации (Framer Motion)
*   **Что сделано:** Плавные fade-in/fade-out переходы между страницами (AnimatePresence), stagger-анимация (поочередное появление) строк в таблице продуктов, плавное изменение ширины сайдбара без скачков верстки, анимация вращения иконок при смене темы.
*   **Обоснование:** Анимации создают ощущение "бесшовности" и отзывчивости приложения. Они помогают пользователю понять контекст происходящего (например, что данные загрузились и появились на экране) и делают взаимодействие с приложением "живым".

### 2. Оптимистичные обновления (Optimistic UI)
*   **Что сделано:** Реализована кнопка удаления продукта. При клике продукт мгновенно исчезает из таблицы (и из кэша RTK Query), не дожидаясь ответа от сервера. Если сервер возвращает ошибку, UI незаметно откатывается назад.
*   **Обоснование:** Этот паттерн создает иллюзию нулевой задержки (zero-latency), что критически важно для UX в админ-панелях.

### 3. Строгая валидация и производительность форм (React Hook Form + Zod)
*   **Что сделано:** Форма авторизации переписана с использования обычных `useState` на связку `react-hook-form` и `zod`.
*   **Обоснование:** Использование неконтролируемых компонентов (RHF) избавляет от лишних ререндеров при каждом нажатии клавиши. Zod обеспечивает строгую статическую типизацию (TypeScript) от схемы валидации до отправки данных на сервер, исключая ошибки человеческого фактора.

### 4. Отказоустойчивость (Error Boundaries)
*   **Что сделано:** Создан компонент `GlobalErrorBoundary`, оборачивающий всё приложение.
*   **Обоснование:** В случае непредвиденной ошибки рендеринга (например, из-за битых данных с сервера), приложение не упадет с "белым экраном смерти". Пользователь увидит красивый fallback-интерфейс с описанием ошибки и кнопкой перезагрузки, что является обязательным требованием для Enterprise-приложений.

### 5. Система уведомлений (Sonner) и Пустые состояния (Empty States)
*   **Что сделано:** Интегрирована библиотека `sonner` для красивых всплывающих toast-уведомлений. Создан переиспользуемый компонент `EmptyState` для отображения заглушек (например, когда поиск не дал результатов).
*   **Обоснование:** Качественный UX требует постоянной обратной связи. Пользователь должен четко понимать результат своих действий (успешный вход, ошибка удаления) и не должен видеть просто пустой экран, если данных нет.

### 6. Семантика и Доступность (a11y)
*   **Что сделано:** Использование семантических HTML-тегов (`main`, `article`, `nav`, `aside`), добавление `aria`-атрибутов, поддержка `prefers-reduced-motion` (отключение анимаций для пользователей, у которых это задано в настройках ОС).
*   **Обоснование:** Делает приложение доступным для скринридеров, улучшает структуру кода и показывает профессиональный подход к веб-разработке.

### 7. Безопасные действия в каталоге (Confirm before delete)
*   **Что сделано:** Перед удалением продукта показывается модальное подтверждение с названием товара, после подтверждения выполняется удаление с optimistic update.
*   **Обоснование:** Снижает риск случайного удаления и делает критические действия в админ-панели предсказуемыми и контролируемыми.

### 8. Улучшенная информационная архитектура и навигация
*   **Что сделано:** Навигация в sidebar разделена на логические секции (`Operations` и `Account`), а на странице деталей продукта добавлен breadcrumb (`Products / Product`).
*   **Обоснование:** Пользователю проще ориентироваться в структуре приложения и понимать текущий контекст без лишних кликов.
